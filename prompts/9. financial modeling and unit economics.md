**ROLE & CONTEXT**

Act as a VP of Finance with 15+ years of experience at deep-tech infrastructure startups and prior consulting at a Tier-1 strategy firm. You think in structured frameworks, communicate with precision, and never present a number without stating the assumption that drives it.

> **Project:** OpenHuman — Pixar-style Digital Human Streaming System
> **Business model:** Freemium infrastructure standard (free tier at 100M+ MAU; monetization via enterprise SDK licensing, B2B embedding, and partnership tiers — similar to Mapbox/Twilio)
> **Stage:** Pre-revenue / MVP (Months 1–16 of a 24-month roadmap)
> **Revenue:** `[FILL: Current MRR/ARR or $0 pre-revenue]`
> **Monthly burn:** `[FILL: Current burn]`
> **Cash on hand:** `[FILL: Current cash]`
> **Growth rate:** `[FILL: MoM active sessions or embedded deployments]`
>
> **Key infrastructure choices driving cost model:**
>
> - WebSocket backend: Elixir/OTP on Hetzner AMD EPYC (no Kubernetes)
> - Cache layer: Custom Tokio Rust KV in-process NIF (target >70% hit rate → avoids GPU spend on cache-hit traffic)
> - CDN/Edge: Cloudflare (maximizing free/cheap tier at MVP)
> - GPU nodes: minority of fleet; CPU-only nodes serve cache hits
> - Protocol: Custom binary WebSocket with delta motion encoding (~100 kbps/session vs. 5 Mbps for realistic 3D)

---

**DELIVERABLE 1 — UNIT ECONOMICS DASHBOARD**

Build a complete unit economics model with explicit formulas and stated assumptions for every metric. Use **active session** as the primary unit (not "user") since OpenHuman is an infrastructure standard embedded by third parties, not a direct-to-consumer subscription.

① **Cost per active session** — broken down by traffic type:

- Cache hit session (CPU-only, Cloudflare edge): estimated cost
- Cache miss session (hits GPU pipeline): estimated cost
- Blended cost per session at target >70% cache hit rate
  _Formula: (Cache Hit % × CPU Cost/session) + (Cache Miss % × GPU Cost/session)_

② **Cost per embedded deployment** (for B2B SDK partners) — infrastructure cost to serve one third-party app's monthly active users. Distinguish chitchat/low-complexity vs. domain-specialized (medical, legal) context trees.

③ **LTV of enterprise embedding partner** — ARPU from SDK licensing tier, gross margin %, churn rate of B2B partners.
_Formula: (ARPU × Gross Margin %) ÷ Monthly Partner Churn Rate_

④ **LTV:CAC ratio and payback period** for enterprise SDK channel. Flag if ratio falls below 3:1 or payback exceeds 18 months.

⑤ **Contribution margin per session** — Revenue (if any) minus: compute (GPU + CPU), bandwidth (Cloudflare + Hetzner egress), storage (R2 + Postgres), and protocol/CDN overhead. Express as both $ and %.

⑥ **Cache hit rate sensitivity** — show how blended cost/session changes at 50%, 70%, 80%, 90% cache hit rates. This is the single most important cost lever in the OpenHuman model.

---

**DELIVERABLE 2 — 3-YEAR FINANCIAL PROJECTION**

Align with the OpenHuman roadmap phases:

- **Year 1 (Months 1–12, Phases 1–5):** Monthly P&L — Infrastructure Spend, R&D (salaries, compute for training), Data Acquisition Cost (Hetzner GPU for OpenHuman-Animator batch jobs), EBITDA, Net Cash Flow. Revenue = $0 until closed beta.
- **Year 2 (Months 13–24, Phases 6–8):** Quarterly rollup — transition to revenue via enterprise SDK licensing. Model the ramp from 1k → 10k → 100k MAU embedded.
- **Year 3:** Quarterly — target 10M+ MAU, multi-region Hetzner, Cloudflare Workers edge compute, partnership SDK revenue (Zoom, Discord tier).

**Cost structure must separate:**

- Fixed: engineering salaries, Hetzner reserved capacity, Cloudflare plan
- Variable: GPU inference cost per cache-miss session, Cloudflare egress per GB, R2 storage per TB, Postgres per row-GB
- One-time: MetaHuman Animator licensing, UE5 infrastructure setup, dataset annotation QA

**Break-even analysis:**

- Session-level: the cache hit rate % at which cost/session reaches $0.001 (free-tier sustainability target)
- Company-level: the month and MAU volume where the business hits contribution margin break-even and EBITDA break-even
- Cash runway: monthly opening/closing cash, burn rate, and runway in months assuming `[FILL: current cash]`

---

**DELIVERABLE 3 — SCENARIO & SENSITIVITY ANALYSIS**

Model three scenarios across OpenHuman's two primary cost levers — **cache hit rate** and **GPU cost/inference**:

| Scenario  | Cache Hit Rate | GPU Cost/inference vs. Base | Purpose                                    |
| --------- | -------------- | --------------------------- | ------------------------------------------ |
| Bear Case | 50%            | +40% (GPU market tightens)  | Survival floor — can we sustain free tier? |
| Base Case | 70%            | Current market rate         | Expected MVP outcome                       |
| Bull Case | 90%            | −30% (Hetzner GPU deal)     | Upside — cost MOAT achieved                |

Also model a secondary axis: **enterprise SDK adoption rate** (slow: 5 partners/quarter vs. fast: 20 partners/quarter) × the three cache scenarios above = 6 combined scenarios.

Show Year 1 and Year 3 impact on: blended cost/session, monthly infrastructure burn, revenue, EBITDA, and cash runway for each scenario.

---

**DELIVERABLE 4 — ASSUMPTION REGISTER**

For every assumption in the model, provide:

`Assumption | Value Used | Source / Justification | Sensitivity Level (High / Medium / Low)`

**Required assumptions to include:**

- Average session duration (minutes)
- Bandwidth per session (target ~100 kbps; compare to 5 Mbps realistic 3D baseline)
- Cache hit rate at MVP launch vs. at scale
- GPU inference time per cache-miss session (target <500ms first-byte)
- Hetzner AMD EPYC box cost/month and concurrent WebSocket connections per box (target 1M+)
- Cloudflare egress cost (GB/month at each MAU milestone)
- MetaHuman Animator throughput (hours of curves extracted per GPU-hour)
- Dataset size at Phase 2 completion (target >10,000 hours)
- OmniVoice fine-tuning compute cost (one-time, Phase 2)
- OpenHuman-Motion training compute cost (one-time, Phase 3)
- Enterprise SDK partner churn rate
- Time-to-first-revenue (assumed Phase 7: Month 16)

---

**DELIVERABLE 5 — BENCHMARKS & RED FLAGS**

Compare key metrics against benchmarks for **open-source infrastructure standards with freemium B2B monetization** (reference: Mapbox, Twilio, Mux, Daily.co, Livekit).

**OpenHuman-specific red flags — flag immediately if:**

- Blended cost/active session exceeds $0.005 (free tier becomes unsustainable at 100M MAU)
- Cache hit rate at MVP launch falls below 50% (pre-compute pipeline insufficient)
- GPU inference latency (first-byte) exceeds 500ms (breaks lip-sync UX requirement)
- Enterprise SDK CAC exceeds `[FILL]` with LTV:CAC below 3:1
- Cloudflare egress cost grows faster than session volume (delta encoding not reducing bandwidth as modeled)
- Data acquisition cost (OpenHuman-Animator GPU batch) exceeds `[FILL]` per hour of extracted curves

For each red flag: state the threshold trigger, the downstream impact on free-tier sustainability, and a concrete corrective action mapped to a specific OpenHuman module (e.g., _"Cache hit rate below 50% → expand pre-compute pipeline in OpenHuman-Brain, increase TTL on chitchat context tree, review OpenHuman-Crawler topic diversity"_).

---

**OUTPUT FORMAT**

Lead with a 3–4 sentence executive summary framed for a Seed or Series A investor: what does the unit economics model reveal about the viability of the free-tier-as-standard strategy, and what is the single most critical assumption to de-risk in the next 90 days? Follow with each deliverable as a clearly labeled section containing tables, formulas, and highlighted callout boxes for red flags. Precision over verbosity — write as if presenting to an investor who has already read the OpenHuman architecture document.
